// o parametro segue a ordem
function cumprimentar(pessoa, idade) { // declsrei a função
    console.log(`Ola, ${pessoa}!  Fiquei sabendo que você é um trouxa de ${idade}`);
}

const nome = 'jose';
cumprimentar(nome, 30);
cumprimentar('Makson', 30);
cumprimentar('Italo', 28);